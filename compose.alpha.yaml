services:
  app:
    container_name: hexagone_six_alpha
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '5000:3000'
    volumes:
      - /mnt/volumes:/app/storage

volumes:
  storage:
